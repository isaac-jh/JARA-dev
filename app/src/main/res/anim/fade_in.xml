<?xml version="1.0" encoding="utf-8"?>
<alpha xmlns:android="http://schemas.android.com/apk/res/android"
    android:interpolator="@android:anim/accelerate_interpolator"
    android:fromAlpha="0.0"
    android:toAlpha="1.0"
    android:duration="@android:integer/config_longAnimTime" />

    <!--
    fade in 사용예제 (fade out도 동일)
    Animation slowly_appear,slowlyDisappear;
    slowlyDisappear = AnimationUtils.loadAnimation(this,R.anim.slowly_fadeout);
    slowly_appear = AnimationUtils.loadAnimation(this,R.anim.slowly_fadein);
    menuLeftList.setAnimation(slowlyDisappear);
    mLayout.setAnimation(slowly_appear);

    -->
